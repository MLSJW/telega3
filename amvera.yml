meta:
  environment: node
  toolchain:
    name: npm
    version: "18"
build:
  skip: false
  additionalCommands: "cd frontend && npm install && npm run build"
  artifacts:
    "*": /
run:
  command: node backend/server.js
  persistenceMount: /data
  containerPort: "80"